{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
  {{ form_start(reviewmanagerConfigurationForm) }}
  <div class="card">
    <h3 class="card-header">
      <i class="material-icons">settings</i> {{ 'Source types'|trans({}, 'Modules.Reviewmanager.Admin') }}
    </h3>
    <div class="card-body">
      <div class="form-wrapper">
        {{ form_widget(reviewmanagerConfigurationForm) }}
      </div>
    </div>
    <div class="card-footer">
      <div class="d-flex justify-content-end">
        <button class="btn btn-primary float-right" id="save-button">
          {{ 'Save'|trans({}, 'Admin.Actions') }}
        </button>
      </div>
    </div>
  </div>
  {{ form_end(reviewmanagerConfigurationForm) }}
{% endblock %}
